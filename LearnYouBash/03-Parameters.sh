#!/bin/bash

############################################################
#
# Simple script which demonstrates the use of parameters
# received at the command line, which can then be used as
# variables in the script and subsequently output to the
# screen via the echo command (stdout).
#
############################################################

echo "1: ${1}"
echo "3: ${3}"
echo "5: ${5}"
